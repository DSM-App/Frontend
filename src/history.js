import { createBrowserHistory } from 'history';

// We will use the hostroy object exported from this component to import histroy object outside of React components

const history = createBrowserHistory();
export default history;
